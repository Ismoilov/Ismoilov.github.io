{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "rm -rf .nuxt .output && nuxt dev",
    "fresh": "rm -rf .nuxt .output && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "nuxt": "^3.16.0"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
